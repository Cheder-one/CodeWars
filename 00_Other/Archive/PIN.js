function getPINs(observed) {
  // Объект с соседними цифрами
  const adjacent = {
    1: ["1", "2", "4"],
    2: ["1", "2", "3", "5"],
    3: ["2", "3", "6"],
    4: ["1", "4", "5", "7"],
    5: ["2", "4", "5", "6", "8"],
    6: ["3", "5", "6", "9"],
    7: ["4", "7", "8"],
    8: ["5", "7", "8", "9", "0"],
    9: ["6", "8", "9"],
    0: ["0", "8"],
  };

  // Массив результата
  let result = [];

  // Функция для генерации комбинаций
  function generatePins(pin) {
    // Если длина пина равна длине наблюдаемого пина, добавить его в результат и выйти из функции
    if (pin.length === observed.length) {
      result.push(pin);
      return;
    }

    // Получить текущую цифру из наблюдаемого пина
    let currentDigit = observed[pin.length];

    // Получить все соседние цифры для текущей цифры
    let neighbors = adjacent[currentDigit];

    // Для каждой соседней цифры вызвать функцию снова с добавлением этой цифры к пину
    for (let neighbor of neighbors) {
      generatePins(pin + neighbor);
    }
  }

  // Вызвать функцию с пустой строкой
  generatePins("");

  // Вернуть результат
  return result;
}
